[package]
name = "recol"
version = "0.1.5"
edition = "2024"

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.9"
serde_json = "1"
fastrand = "2"
strsim = "0.11"
